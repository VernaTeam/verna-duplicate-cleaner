# Required: the window itself is an HTML page rendered by WebView2.
pywebview>=6.0     # needs pythonnet, which pip pulls in on Windows

# Optional: the app runs without these, with fewer abilities.
mutagen>=1.47      # reads music tags; without it the tag/duration methods are off
send2trash>=1.8    # moves deletions to the Recycle Bin instead of erasing them

# Build-only, not needed to run from source:
# pyinstaller>=6.0
# pillow>=10.0     # used by build_tools/make_icon.py to draw app.ico
